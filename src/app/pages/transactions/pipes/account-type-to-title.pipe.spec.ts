import { AccountTypeToTitlePipe } from './account-type-to-title.pipe';

describe('AccountTypeToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountTypeToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
