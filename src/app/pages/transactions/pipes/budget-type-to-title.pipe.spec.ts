import { BudgetTypeToTitlePipe } from './budget-type-to-title.pipe';

describe('BudgetTypeToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetTypeToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
